import 'package:flutter/material.dart';

class MixerPage extends StatelessWidget {
  const MixerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text('Mixer Page'),
      );
  }
}