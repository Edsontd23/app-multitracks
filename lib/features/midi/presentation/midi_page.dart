import 'package:flutter/material.dart';

class MidiPage extends StatelessWidget {
  const MidiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text('MIDI Page'),
      );
  }
}