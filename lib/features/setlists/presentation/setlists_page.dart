import 'package:flutter/material.dart';

class SetlistsPage extends StatelessWidget {
  const SetlistsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text('Setlist Page'),
      );
  }
}